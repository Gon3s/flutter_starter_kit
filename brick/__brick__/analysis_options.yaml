include: package:very_good_analysis/analysis_options.10.2.0.yaml

analyzer:
  exclude: [
    build/**,
    lib/**.freezed.dart,
    lib/**.g.dart,
  ]

linter:
  rules:
    lines_longer_than_80_chars: false
